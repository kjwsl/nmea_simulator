{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Program",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/nmea_simulator",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}"
        }
    ]
}
