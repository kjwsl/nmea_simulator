{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Program",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/rs",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}"
        }
    ]
}
